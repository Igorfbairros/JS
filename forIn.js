// const frutas = ['Manga', 'Maçã', 'Uva'];

// for(let i in frutas){
//     console.log(frutas[i])
// }

const pessoa = {
    nome: 'Luiz',
    sobrenome: 'Otavio',
    idade: 30
};

for(let key in pessoa){
    console.log(key, pessoa[key]);
}
