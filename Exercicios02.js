// Write a function called ePaisagem that receive two arguments, width  and height of image (number).
//And  return true if the photo is in landscape mode (modo paisagem) 

const ePaisagem = (width, height) => width >= height // ? true : false;



console.log(ePaisagem(100, 30));